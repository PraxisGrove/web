# PraxisGrove Frontend - 核心功能展示实施计划

> **方案**：方案1 - 完善核心功能展示  
> **目标**：补全前端核心功能，打造完整的产品演示  
> **预计时间**：3-4周  
> **开始日期**：2025-10-11

---

## 📋 总体目标

打造一个功能完整、交互流畅的前端产品演示，使用 Mock 数据实现所有核心功能，为后续后端集成做好准备，并优化 Vercel 部署配置。

### 核心交付物

1. ✅ **课程展示系统** - 完整的课程浏览、搜索、详情展示
2. ✅ **3D知识宇宙** - 可视化的知识图谱演示
3. ✅ **AI助手界面** - 智能对话UI与Mock响应
4. ✅ **社区页面框架** - 讨论区基础UI
5. ✅ **Mock数据系统** - 完善的测试数据
6. ✅ **Vercel部署优化** - 生产环境配置

---

## 🗓 详细实施计划

### 阶段1：课程展示系统开发（Week 1-2）

#### 目标

构建完整的课程浏览和展示系统，包括列表、详情、搜索、筛选等功能。

#### 1.1 课程数据模型设计（Day 1）

**创建文件**：

- `src/types/course.ts` - 课程类型定义
- `src/lib/mock-data/courses.ts` - Mock课程数据

**类型定义**：

```typescript
interface Course {
  id: string;
  title: string;
  description: string;
  instructor: {
    id: string;
    name: string;
    avatar: string;
  };
  thumbnail: string;
  category: string;
  level: 'beginner' | 'intermediate' | 'advanced';
  duration: number; // 分钟
  price: number;
  rating: number;
  studentCount: number;
  chapters: Chapter[];
  tags: string[];
  createdAt: string;
  updatedAt: string;
}

interface Chapter {
  id: string;
  title: string;
  duration: number;
  lessons: Lesson[];
}

interface Lesson {
  id: string;
  title: string;
  type: 'video' | 'article' | 'quiz';
  duration: number;
  isPreview: boolean;
}
```

**Mock数据量**：

- 50-100 个课程
- 涵盖 5-8 个类别
- 包含完整的章节和课时信息

#### 1.2 课程列表页面（Day 2-3）

**创建文件**：

- `src/app/(main)/courses/page.tsx` - 课程列表页
- `src/components/courses/CourseCard.tsx` - 课程卡片
- `src/components/courses/CourseFilters.tsx` - 筛选组件
- `src/components/courses/CourseSearch.tsx` - 搜索组件

**功能实现**：

- 网格/列表视图切换
- 分类筛选（前端、后端、设计等）
- 难度筛选
- 价格筛选
- 评分筛选
- 搜索功能
- 排序（最新、最热、评分）
- 分页/无限滚动

**UI设计**：

- 响应式网格布局（1/2/3/4列）
- 卡片悬停动画
- 骨架屏加载
- 空状态处理

#### 1.3 课程详情页面（Day 4-5）

**创建文件**：

- `src/app/(main)/courses/[id]/page.tsx` - 课程详情
- `src/components/courses/CourseHeader.tsx` - 课程头部
- `src/components/courses/CourseCurriculum.tsx` - 课程大纲
- `src/components/courses/CourseReviews.tsx` - 课程评价
- `src/components/courses/CourseInstructor.tsx` - 讲师信息
- `src/components/courses/RelatedCourses.tsx` - 相关课程

**页面结构**：

```
┌─────────────────────────────────────┐
│  课程标题 & 基本信息                 │
│  [预览视频]                         │
│  ├─ 评分、学生数、时长               │
│  └─ 加入购物车 / 立即购买             │
├─────────────────────────────────────┤
│  Tabs导航                           │
│  ├─ 概述                            │
│  ├─ 课程大纲                        │
│  ├─ 讲师介绍                        │
│  ├─ 评价 (⭐⭐⭐⭐⭐)                │
│  └─ 常见问题                        │
├─────────────────────────────────────┤
│  相关推荐课程                       │
└─────────────────────────────────────┘
```

**功能实现**：

- 课程大纲展开/折叠
- 预览课时标记
- 加入购物车
- 加入收藏
- 分享功能
- 评价展示与筛选
- 相关课程推荐

#### 1.4 课程搜索功能（Day 6）

**创建文件**：

- `src/app/(main)/search/page.tsx` - 搜索结果页
- `src/components/search/SearchBar.tsx` - 增强搜索栏
- `src/components/search/SearchSuggestions.tsx` - 搜索建议
- `src/lib/utils/search.ts` - 搜索工具函数

**功能实现**：

- 实时搜索建议
- 搜索历史
- 热门搜索
- 高级筛选
- 搜索结果高亮
- 无结果提示

#### 1.5 课程API服务层（Day 7）

**创建文件**：

- `src/services/course.service.ts` - 课程服务
- `src/hooks/useCourses.ts` - 课程Hooks
- `src/store/course.ts` - 课程状态管理

**API方法**：

```typescript
// 获取课程列表
getCourses(filters?: CourseFilters): Promise<Course[]>

// 获取课程详情
getCourseById(id: string): Promise<Course>

// 搜索课程
searchCourses(query: string): Promise<Course[]>

// 获取相关课程
getRelatedCourses(courseId: string): Promise<Course[]>

// 获取推荐课程
getRecommendedCourses(): Promise<Course[]>
```

---

### 阶段2：3D知识宇宙基础版（Week 2-3）

#### 目标

实现可视化的3D知识图谱，展示课程之间的关联和学习路径。

#### 2.1 3D场景基础搭建（Day 8-9）

**更新文件**：

- `src/app/knowledge-universe/page.tsx` - 完善页面
- `src/components/3d/knowledge-graph.tsx` - 完善3D图谱
- `src/components/3d/KnowledgeNode.tsx` - 知识节点组件
- `src/components/3d/NodeConnection.tsx` - 连接线组件

**技术实现**：

```typescript
// 使用 React Three Fiber
import { Canvas } from '@react-three/fiber';
import { OrbitControls, PerspectiveCamera } from '@react-three/drei';

// 3D场景配置
const Scene = () => (
  <Canvas>
    <PerspectiveCamera makeDefault position={[0, 0, 50]} />
    <OrbitControls />
    <ambientLight intensity={0.5} />
    <pointLight position={[10, 10, 10]} />
    {/* 知识节点 */}
    <KnowledgeNodes />
    {/* 连接线 */}
    <NodeConnections />
  </Canvas>
);
```

**节点设计**：

- 球体表示课程/主题
- 大小表示重要性
- 颜色表示类别
- 发光效果表示已完成

#### 2.2 数据模型与布局算法（Day 10）

**创建文件**：

- `src/types/knowledge-graph.ts` - 知识图谱类型
- `src/lib/graph-layout.ts` - 布局算法
- `src/lib/mock-data/knowledge-graph.ts` - Mock图谱数据

**类型定义**：

```typescript
interface KnowledgeNode {
  id: string;
  title: string;
  type: 'course' | 'topic' | 'skill';
  category: string;
  position: [number, number, number];
  size: number;
  color: string;
  completed: boolean;
  connections: string[]; // 连接的节点ID
}

interface KnowledgeGraph {
  nodes: KnowledgeNode[];
  connections: Connection[];
}

interface Connection {
  from: string;
  to: string;
  type: 'prerequisite' | 'related' | 'advanced';
  strength: number;
}
```

**布局算法**：

- 力导向布局（Force-directed layout）
- 分层布局（Hierarchical layout）
- 球形布局（Spherical layout）

#### 2.3 交互功能实现（Day 11-12）

**创建文件**：

- `src/components/3d/NodeTooltip.tsx` - 节点提示
- `src/components/3d/NodePanel.tsx` - 节点详情面板
- `src/components/3d/GraphControls.tsx` - 图谱控制器

**交互功能**：

- 点击节点查看详情
- 悬停显示基本信息
- 高亮学习路径
- 缩放和旋转
- 视角切换（俯视/侧视/自由）
- 节点搜索定位

#### 2.4 性能优化（Day 13）

**优化策略**：

- 节点LOD（Level of Detail）
- 视锥剔除
- 实例化渲染
- 按需加载节点数据
- WebGL性能监控

**创建文件**：

- `src/lib/3d-performance.ts` - 3D性能优化工具

---

### 阶段3：AI助手界面实现（Week 3）

#### 目标

构建智能对话界面，实现AI助手的前端交互体验。

#### 3.1 对话界面搭建（Day 14-15）

**创建文件**：

- `src/app/(main)/ai/page.tsx` - AI助手页面
- `src/components/ai/ChatContainer.tsx` - 聊天容器
- `src/components/ai/MessageList.tsx` - 消息列表
- `src/components/ai/MessageBubble.tsx` - 消息气泡
- `src/components/ai/InputArea.tsx` - 输入区域
- `src/components/ai/QuickActions.tsx` - 快捷操作

**页面布局**：

```
┌─────────────────────────────────────┐
│  AI助手标题 & 状态                   │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │  欢迎消息                    │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │  用户: 问题                  │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │  AI: 回答...                 │   │
│  │  [正在输入...]               │   │
│  └─────────────────────────────┘   │
│                                     │
│  快捷操作                           │
│  [推荐课程] [学习路径] [答疑]       │
├─────────────────────────────────────┤
│  [输入框]                    [发送] │
└─────────────────────────────────────┘
```

**UI特性**：

- 打字机效果
- Markdown支持
- 代码高亮
- 图片/链接预览
- 滚动到底部

#### 3.2 AI响应模拟（Day 16）

**创建文件**：

- `src/services/ai.service.ts` - AI服务
- `src/lib/mock-data/ai-responses.ts` - Mock响应数据
- `src/hooks/useAI.ts` - AI Hooks

**Mock响应策略**：

```typescript
// 基于关键词的智能回复
const responses = {
  推荐课程: generateCourseRecommendations(),
  学习路径: generateLearningPath(),
  问题: searchKnowledgeBase(),
  进度: getUserProgress(),
  // ...
};

// 流式响应模拟
async function* streamResponse(message: string) {
  const response = await getAIResponse(message);
  for (const char of response) {
    yield char;
    await delay(20); // 打字机效果
  }
}
```

**AI功能**：

- 课程推荐
- 学习路径规划
- 问题答疑
- 进度查询
- 资源推荐

#### 3.3 上下文管理（Day 17）

**创建文件**：

- `src/store/chat.ts` - 聊天状态管理
- `src/lib/context-manager.ts` - 上下文管理器

**功能实现**：

- 对话历史记录
- 上下文关联
- 会话持久化
- 多会话管理
- 历史对话搜索

---

### 阶段4：社区页面框架（Week 4）

#### 目标

构建社区讨论区的基础UI框架。

#### 4.1 社区首页（Day 18）

**创建文件**：

- `src/app/(main)/community/page.tsx` - 社区首页
- `src/components/community/PostCard.tsx` - 帖子卡片
- `src/components/community/CategoryFilter.tsx` - 分类筛选
- `src/components/community/TrendingTopics.tsx` - 热门话题

**页面结构**：

```
┌─────────────────────────────────────┐
│  社区导航 & 搜索                     │
├─────────────────────────────────────┤
│  [分类筛选]  [排序]  [+ 发帖]       │
├─────────────────────────────────────┤
│  热门话题                           │
│  [#前端开发] [#AI学习] [#职业规划]   │
├─────────────────────────────────────┤
│  帖子列表                           │
│  ┌─────────────────────────────┐   │
│  │ [头像] 用户名 · 1小时前      │   │
│  │ 标题                         │   │
│  │ 摘要...                      │   │
│  │ [💬 23] [👍 45] [🔖 12]      │   │
│  └─────────────────────────────┘   │
│  ...更多帖子                        │
└─────────────────────────────────────┘
```

#### 4.2 帖子详情页（Day 19）

**创建文件**：

- `src/app/(main)/community/[id]/page.tsx` - 帖子详情
- `src/components/community/PostContent.tsx` - 帖子内容
- `src/components/community/CommentSection.tsx` - 评论区
- `src/components/community/CommentItem.tsx` - 评论项

**功能实现**：

- Markdown渲染
- 代码高亮
- 点赞/收藏
- 评论/回复
- 分享功能

#### 4.3 发帖编辑器（Day 20）

**创建文件**：

- `src/components/community/PostEditor.tsx` - 发帖编辑器
- `src/components/community/EditorToolbar.tsx` - 工具栏

**编辑器功能**：

- Markdown编辑
- 实时预览
- 图片上传
- 标签选择
- 草稿保存

---

### 阶段5：Mock数据完善与集成测试（Week 4）

#### 5.1 Mock数据服务（Day 21）

**创建文件**：

- `src/lib/mock-data/index.ts` - 统一导出
- `src/lib/mock-api.ts` - Mock API服务
- `src/lib/mock-delay.ts` - 模拟网络延迟

**Mock API特性**：

- 模拟网络延迟（200-500ms）
- 随机失败场景（可配置）
- 分页支持
- 搜索过滤
- 排序功能

#### 5.2 数据完善（Day 22）

**完善内容**：

- 100+ 课程数据
- 50+ 社区帖子
- 200+ 评论数据
- 20+ 讲师信息
- 知识图谱数据（100+ 节点）
- AI对话模板（50+ 场景）

#### 5.3 集成测试（Day 23）

**测试内容**：

- 课程浏览流程
- 搜索功能
- 3D图谱交互
- AI对话流程
- 社区互动

**创建文件**：

- `e2e/course-flow.spec.ts` - 课程流程测试
- `e2e/search.spec.ts` - 搜索测试
- `e2e/ai-chat.spec.ts` - AI对话测试

---

### 阶段6：Vercel部署配置优化（Week 4）

#### 6.1 Vercel配置文件（Day 24）

**创建文件**：

- `vercel.json` - Vercel配置
- `.env.production` - 生产环境变量
- `next.config.production.js` - 生产配置

**配置内容**：

```json
{
  "buildCommand": "pnpm build",
  "outputDirectory": ".next",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "regions": ["hkg1", "sin1"],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        }
      ]
    }
  ]
}
```

#### 6.2 性能优化（Day 25）

**优化项目**：

- 启用 ISR（增量静态再生成）
- 配置边缘缓存
- 图片优化（Vercel Image Optimization）
- 字体优化
- Bundle分析与优化

**创建文件**：

- `scripts/analyze-bundle.js` - Bundle分析脚本
- `docs/PERFORMANCE.md` - 性能优化文档

#### 6.3 监控与分析（Day 26）

**集成服务**：

- Vercel Analytics
- Web Vitals监控
- 错误追踪（Sentry）
- 日志收集

**创建文件**：

- `src/lib/analytics.ts` - 分析工具
- `src/lib/error-tracking.ts` - 错误追踪

#### 6.4 CI/CD配置（Day 27）

**创建文件**：

- `.github/workflows/ci.yml` - CI工作流
- `.github/workflows/deploy.yml` - 部署工作流
- `docs/DEPLOYMENT.md` - 部署文档

**工作流**：

```yaml
name: CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
      - run: pnpm install
      - run: pnpm lint
      - run: pnpm type-check
      - run: pnpm test

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: vercel/action@v1
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
```

#### 6.5 文档完善（Day 28）

**创建/更新文件**：

- `docs/FEATURES.md` - 功能清单
- `docs/MOCK_DATA.md` - Mock数据说明
- `docs/API.md` - API文档
- `README.md` - 更新项目说明

---

## 📊 技术栈补充

### 新增依赖

```json
{
  "dependencies": {
    "@react-three/fiber": "^8.17.0",
    "@react-three/drei": "^9.100.0",
    "three": "^0.167.0",
    "react-markdown": "^9.0.0",
    "remark-gfm": "^4.0.0",
    "highlight.js": "^11.9.0"
  },
  "devDependencies": {
    "@vercel/analytics": "^1.2.0",
    "@sentry/nextjs": "^7.100.0"
  }
}
```

### 组件库使用

- **shadcn/ui**: 基础UI组件
- **Framer Motion**: 页面过渡和动画
- **React Three Fiber**: 3D渲染
- **Recharts**: 数据可视化（统计图表）

---

## 🎯 质量保证

### 代码规范

- TypeScript strict模式
- ESLint检查通过
- Prettier格式化
- 组件Props类型完整

### 性能指标

- 首屏加载 < 2s
- LCP < 2.5s
- CLS < 0.1
- FID < 100ms
- Bundle size < 2MB

### 可访问性

- ARIA标签完整
- 键盘导航支持
- 颜色对比度 >= 4.5:1
- 屏幕阅读器友好

---

## 📝 交付清单

### 代码交付

- [ ] 所有功能代码提交到 Git
- [ ] 通过 ESLint 和 TypeScript 检查
- [ ] Mock 数据完整可用
- [ ] E2E 测试通过

### 文档交付

- [ ] 功能文档（FEATURES.md）
- [ ] API文档（API.md）
- [ ] Mock数据文档（MOCK_DATA.md）
- [ ] 部署文档（DEPLOYMENT.md）
- [ ] 性能优化文档（PERFORMANCE.md）

### 部署交付

- [ ] Vercel 生产环境部署成功
- [ ] Analytics 配置完成
- [ ] 错误监控配置完成
- [ ] CI/CD 流程运行正常

---

## 🔄 后续计划

完成方案1后，可选择：

1. **方案2**：提升项目质量（测试覆盖率、文档完善）
2. **方案3**：深度优化（性能、SEO、用户体验）
3. **后端集成**：连接真实API，替换Mock数据

---

**创建日期**：2025-10-11  
**最后更新**：2025-10-11  
**状态**：规划完成，等待实施
